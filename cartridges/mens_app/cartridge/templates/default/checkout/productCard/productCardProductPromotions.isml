<isif condition="${lineItem.appliedPromotions}">
    <isloop items="${lineItem.appliedPromotions}" var="appliedPromotion">
        <div>
            <isprint value="${appliedPromotion.callOutMsg.replace("de descuento", "")}" encoding="off"/>
        </div>
    </isloop>
</isif>
